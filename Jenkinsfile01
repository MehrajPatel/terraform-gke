pipeline {
    agent any

    stages {
    stage('Terraform init') {
            steps {
                cd ./infra/terraform/
                terraform init
                }
        } 
     stage('Terraform deploy') {
            steps {
                cd ./infra/terraform/
                terraform deploy --auto-approve
           }
        } 
    }
 }
